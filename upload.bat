@echo off
echo This legacy upload script is disabled because it embedded credentials and started an obsolete server entry point.
echo Follow DEPLOYMENT_RUNBOOK.md with an explicitly reviewed target.
exit /b 2
