# Copy this file to .env or configure the same variables in systemd/cloud secrets.
# Do not commit real secrets.

FLASK_SECRET_KEY=replace-with-random-secret-key
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-strong-admin-password

# Local development only. Do not enable on public servers.
ALLOW_DEFAULT_ADMIN=false

